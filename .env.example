# Copy to `.env` (never commit `.env`). Restart `npm run dev` after changes.

# Persistent application storage (Turso / libSQL)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Members portal (/members)
# MEMBERS_PASSWORD=

# Admin applications dashboard (/admin) — required for admin login
ADMIN_PASSWORD=
